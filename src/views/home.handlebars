<header>
    <!-- Sección navbar -->
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Made In Heaven</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/api/products">Productos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/realtimeproducts">Productos en tiempo real</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/chat">Chat</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

<div class="container" id='containerItems'>
    <div class="row">
        <div class="col">
            <div id="contenedorProductos" class="row row-cols-1 row-cols-md-3 g-4">
                {{#each productos}}
                <div id="productoItem" class="card">
                    <img src="{{this.thumbnail}}" class="card-img-top" alt="{{this.title}}">
                    <div class="card-body">
                        <span>
                            <h2 style="font-size: 30px; margin-top: -5px;">{{this.title}}</h2>
                        </span>
                        <span>
                            <p style="margin-top: -5px;">${{this.price}}</p>
                        </span>
                        <button id="btnComprar" class="btn btn-dark" style="margin-top: -10px;">Comprar</button>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
</div>